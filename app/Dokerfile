# Используем официальный образ Python в качестве базового образа
FROM python:3.8-slim

# Устанавливаем переменную окружения AUTHOR по умолчанию
ENV AUTHOR="Alex"

# Копируем код внутрь контейнера
COPY listen.py /app/listen.py

# Устанавливаем рабочую директорию
WORKDIR /app

# Устанавливаем зависимости, необходимые для работы скрипта
RUN pip install --no-cache-dir http.server

# Открываем порт, на котором будет работать приложение
EXPOSE 8000

# Запускаем скрипт при старте контейнера
CMD ["python", "listen.py"]